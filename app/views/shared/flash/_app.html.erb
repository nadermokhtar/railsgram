<% flash.each do |key,value|%>
  <div class="alert <%= bootstrap_class_for(key)%> alert-dismisable">
  <div class="contiainer">
  <%= value %>
  <button type="button" class="close" data-dismiss="alert">
    <span class="pointerless" aria-hidden="true">&times;</span>
  </button>
  </div>
  </div>
<% end %>